# 复制为 .env.docker 后在 VPS 上修改。
# seed 会清空现有业务数据，只允许在全新的演示环境中临时开启一次。
SEED_ON_BOOT=false
ALLOW_DESTRUCTIVE_SEED=false

# 仅旧版 db push 创建的现有数据库首次切换 migration 时临时设为 true，成功后必须改回 false。
BASELINE_INITIAL_MIGRATION=false

# Docker 生产环境禁止 mock，必须填写当前小程序的 AppID 和 AppSecret。
WECHAT_MOCK=false
WECHAT_APPID=wx9d6ad12cc47b03c7
WECHAT_SECRET=

# 使用 openssl rand -hex 32 生成。
JWT_SECRET=
JWT_EXPIRES_IN=7d

# 浏览器允许的来源，多个域名用英文逗号分隔；小程序请求不受浏览器 CORS 限制。
WEB_ORIGIN=https://console.example.com
LOG_LEVEL=info

TZ=Asia/Shanghai
ADMIN_INIT_USERNAME=admin
ADMIN_INIT_PASSWORD=
