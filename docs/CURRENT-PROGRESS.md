# 小糖本（糖迹）项目当前进度

> 最后更新：2026-09-05（Asia/Shanghai）
>
> 继续开发分支：`codex/wechat-personal-release`
>
> 本轮状态：功能实现完成，用户已授权提交、推送并更新 ECS 后端与网页；小程序由用户自行上传、真机验收及提交审核。实际发布结果见后续更新。

## 1. 当前产品策略

- 微信小程序“小糖本”是主要用户入口；用户已反馈小程序审核通过。
- 手机网页继续保留，但不作为本轮主要推广入口。
- 小程序定位为个人健康数据记录工具，保留游客浏览，不提供药房、售药、问诊、诊断或治疗功能。
- 管理后台及既有药房功能继续保留，与小程序用户界面分开。
- 本轮明确不做“旧网页账号与微信账号关联”，不自动合并账号或记录。
- 生产环境已有真实用户持续录入数据，不得执行生产 `seed`、`docker compose down -v` 或未备份的数据库替换。

## 2. 本轮完成的五项调整

1. **录入与首页显示一致**：手选餐一／餐二后，修改日期时间不会覆盖选择；常用时段优先显示，其余可展开。首页参考范围、状态色带及单位与记录时段、用户设置一致；最近记录明确显示日期。
2. **记录可修改、失败可重试**：历史记录支持修改血糖、血压、血脂、尿酸；未保存的新建内容在本机按账号保留最多 7 天。新建请求使用稳定请求号，失败重试不重复入库；已删除记录不会因重试复活。新增、修改、删除、恢复与日汇总保持事务一致。
3. **统计更清楚**：血糖支持全部／空腹／餐一／餐二；显示完整 7／30／90 天日均值及逐笔明细，缺测明确标出。血压同时展示收缩压与舒张压，血脂四项可单独查看；加载失败可重试。
4. **后台直接查用户与记录**：平台管理员登录后进入“用户与记录”，支持搜索昵称、用户 ID、管理员备注，查看注册时间、最近录入及 7／30 天和全部有效记录次数；详情可按四类指标查看数值。管理员备注不修改用户昵称。
5. **近 7 天大字图片**：“统计”和“我的”增加图片入口，按指标及血糖时段分组、完整分页，每张最多 8 条；支持预览、主动转发、保存相册。预览及转发不要求相册权限，仅保存相册时申请相关授权。

餐一／餐二继续使用用户现有餐后参考范围，没有新增医学诊断或个性化治疗建议。

## 3. 验证状态

- Shared、API、小程序最终回归：13 个测试文件、220 项测试全部通过，包含相册授权、取消、保存失败及反向代理信任场景。
- pnpm 10.33.1 冻结安装、标准 lint／test／build 全部通过；生产与全依赖安全审计均无已知漏洞（npm 官方 registry）。
- 最终 Playwright 浏览器流程 10／10 通过，覆盖登录、注册、录入、后台、导出及回收站；使用独立临时数据库，测试服务已清理。
- Shared、API、管理后台、手机 Web 的 TypeScript 检查通过；API、管理后台、Web 生产构建通过。
- 全新隔离数据库的 5 个 migration 应用成功，迁移结果与 Prisma schema 无差异。
- 本机微信编译器检查首页、历史、录入、统计、周图片、“我的”的 WXML／WXSS 及隐私页 WXML 通过。
- 使用隔离演示数据库，浏览器验收后台登录、用户列表、详情、管理员备注、搜索及指标筛选通过。
- 使用相同绘图函数生成周记录 PNG，检查了血糖和血脂图片，分页内容完整、文字未越界。
- `git diff --check` 通过。
- 尚未完成微信真机点击验收；自动化写入测试仅使用隔离数据库。部署前已确认线上旧服务健康，现有 4 个迁移已成功应用。

## 4. 发布范围与顺序

本轮不能只更新小程序，需要同步更新 **后端 API、管理网页前端、小程序**。现有网页餐一／餐二映射也有改动，可随前端容器发布。

1. 审查改动并完成发布检查后提交、推送。按用户本次要求先更新服务器，再由用户验收和提交小程序。
2. 发布前先执行现有备份流程，保留上一版镜像；详见 [Docker 运维说明](DOCKER-OPERATIONS.md)。
3. 更新后端并确认新迁移 `20260905000000_record_reliability_admin_notes` 成功：新增管理员备注字段及防重复提交凭证表。现有 Docker API 入口会运行 `prisma migrate deploy`；不要用 `db push` 或 seed 代替生产迁移。
4. 更新管理网页前端，确认管理员权限、用户列表、记录详情及备注保存正常。
5. 微信后台核对隐私指引中“保存图片到相册”的用途，与小程序内隐私政策一致；拒绝相册权限不应影响记录、预览、转发。
6. 后端验证正常后，再上传小程序体验版并提交审核；旧客户端仍可使用不带请求号的记录接口。

### 发布准备与依赖检查

已移除 `pnpm-workspace.yaml` 中无效的 `allowBuilds` 占位块，保留既有四项 `onlyBuiltDependencies` 构建许可。使用项目指定的 pnpm 10.33.1，不依赖机器 PATH 中可能存在的 pnpm 11。

发布检查发现并修补已有同主版本依赖漏洞：Fastify、fast-uri、find-my-way、brace-expansion、nanoid、PostCSS、browserslist、qs；两套网页中未使用的 `react-router-dom` 已移除，避免不必要的主版本迁移。锁文件随代码提交。Fastify 的代理信任同时校验内部代理地址与跳数，保持现有 loopback／Docker 网络部署边界。

```bash
corepack pnpm install --frozen-lockfile
corepack pnpm --filter @tangji/shared build
corepack pnpm lint
corepack pnpm test
corepack pnpm build
```

API 运行会读取 Shared 的构建产物，修改 Shared 后必须先构建，避免读到旧的餐一／餐二枚举。

## 5. 真机验收清单

- 游客可浏览；登录后正常新增记录，不要求用户先授权头像或昵称才能浏览。
- 手选餐一／餐二后修改时间，选项不变；首页数值、范围与历史一致。
- 新建内容未保存退出页面后恢复；断网保存失败后重试仅产生一条记录；切换账号不显示上个账号草稿。
- 四类记录均能修改；跨日期修改、删除和恢复后，历史及统计同步变化。
- 空腹／餐一／餐二和 7／30／90 天切换正确，缺测日与空状态清楚。
- 近 7 天图片多页完整；微信转发、保存相册、拒绝授权及取消操作均正常。

## 6. 继续开发及运维信息

- 小程序目录：`apps/wechat-miniprogram`；用微信开发者工具导入此目录。
- 网页及 API 域名：<https://tangji.aiteam.pw>；健康检查：<https://tangji.aiteam.pw/health>。
- 部署信息：ECS `8.152.100.100`，部署目录 `/opt/tangji-v3-deploy`。发布前 API 为 `9bdf345dcd85`，前端为 `cca776628c21ae15c149d517fe41b47a85017bdf`，两个容器均健康；本次更新后仍保留旧镜像供回滚。
- ICP 备案号：`京ICP备2026047601号-1`，网页底部已有展示。
- 继续开发前先拉取 `origin/codex/wechat-personal-release`，再按本文件核对实际发布状态。
- 本地浏览器验证脚本：`scripts/verify-user-records.mjs`、`scripts/verify-weekly-report.mjs`；仅使用隔离测试数据，不要指向生产环境运行写入测试。
- 不要提交 `.env.docker`、微信 AppSecret、JWT 密钥、SSH 私钥、数据库或备份包。
- 用户健康数据属于敏感个人信息，不在文档、日志或提交中记录真实用户明细。
